#!/usr/bin/with-contenv bash

echo 'Check if /config/config.json exists'
[[ ! -e /config/config.json ]] && \
	echo '/config/config.json does not exists' && \
	/app/repertory -gc
#	cp /defaults/config.json /config/config.json

# permissions
chown -R abc:abc \
	/config
